@model IEnumerable<Attendancem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubjectId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AttendanceId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubjectId)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.AttendanceId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.AttendanceId }) |
                    <form asp-action="Delete" asp-route-id="@item.AttendanceId" method="post" style="display:inline">
                        <button type="submit" class="btn btn-link">Delete</button>
                        </form>
            </td>
        </tr>
}
    </tbody>
</table>
